import React from 'react';
import { Link } from 'react-router-dom';

const Header: React.FC = () => {
  return (
    <header>
      <h1>Professor Beta</h1>
      <img src="/path/to/your/logo.png" alt="Logo" className="logo" />
    </header>
  );
};

export default Header;
